import sys

n = int(sys.stdin.readline().rstrip())
sa = [[]]

for i in range(n):
    a, b = map(str, sys.stdin.readline().rstrip().split())
    sa.append([int(a), b])

print(sorted(sa, key=lambda x: x[0]))
