#include <bits/stdc++.h>

using namespace std;

/*
省略前 	省略後
Tokyo University of Agriculture and Technology 	TUAT
Formal Power Series 	FPS
Transmission Control Protocol 	TCP
Uniform Resource Locator 	URL
Central Processing Unit 	CPU
Very Long Instruction Word 	VLIW
Redundant Arrays of Inexpensive Disks 	RAID
Static Random Access Memory 	SRAM
Read Only Memory 	ROM
*/

int main() {
    string s;
    cin >> s;

    if (s == "Tokyo") cout << "TUAT" << endl;
    if (s == "Formal") cout << "FPS" << endl;
    if (s == "Transmission") cout << "TCP" << endl;
    if (s == "Uniform") cout << "URL" << endl;
    if (s == "Central") cout << "CPU" << endl;
    if (s == "Very") cout << "VLIW" << endl;
    if (s == "Redundant") cout << "RAID" << endl;
    if (s == "Static") cout << "SRAM" << endl;
    if (s == "Read") cout << "ROM" << endl;
}
