#include <bits/stdc++.h>

using namespace std;

int main(void) {
    string lstr, rstr;
    cin >> lstr >> rstr;

    uint64_t l_digit = lstr.size() - 1;
    int l_first = lstr[0] - '0';

    uint64_t r_digit = rstr.size() - 1;
    int r_first = rstr[0] - '0';

    return 0;
}
