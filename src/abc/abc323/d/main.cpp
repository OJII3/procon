#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<pair<int, int>> sc(n);
  for (int i = 0; i < n; i++) {
    cin >> sc[i].first >> sc[i].second;
  }

  for (int i = 0; i < n; ++i) {
    if (sc[i].second >= 2) {
    }
  }

  return 0;
}
