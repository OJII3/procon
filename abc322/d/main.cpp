#include <bits/stdc++.h>

using namespace std;

int main() {
  vector<string> p1(4), p2(4), p3(4);
  for (int i = 0; i < 4; ++i) {
    cin >> p1[i];
  }
  for (int i = 0; i < 4; ++i) {
    cin >> p2[i];
  }
  for (int i = 0; i < 4; ++i) {
    cin >> p3[i];
  }
}
